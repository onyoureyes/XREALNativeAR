# ═══ XREAL Orchestrator v2 Dependencies ═══
# 기존 orchestrator 의존성 + LangChain/LangGraph/CrewAI

# ─── 웹 프레임워크 ───
fastapi==0.115.0
uvicorn==0.30.0
httpx==0.27.0
pydantic>=2.0
python-dotenv==1.0.1

# ─── LangChain 코어 ───
langchain-core>=0.3.0
langchain-openai>=0.2.0       # ChatOpenAI (llama.cpp 호환)
langchain-community>=0.3.0    # 추가 도구/유틸

# ─── LangGraph (이야기꾼 상태 그래프) ───
langgraph>=0.2.0
langgraph-checkpoint>=2.0.0   # 체크포인트 (서버 재시작 시 상태 복원)

# ─── CrewAI (전문가 팀) ───
crewai>=0.80.0
crewai-tools>=0.14.0          # 빌트인 도구 (선택)

# ─── 기억 시스템 ───
mem0ai>=0.1.0
chromadb>=0.4.0
sentence-transformers>=2.2.0

# ─── 마이닝 (기존 유지) ───
bertopic>=0.16.0
hdbscan>=0.8.33
umap-learn>=0.5.0
stumpy>=1.12.0
numpy>=1.24.0
graphiti-core[kuzu]>=0.5.0

# ─── 예측 (기존 유지) ───
neuralprophet>=0.8.0
pytorch-forecasting>=1.0.0
pandas>=2.0.0
